#include <iostream>

using namespace std;
using Bynary Operation = int(*)(int)

int f (int* arr; int first; int last; int result; BO operation)
{
    for ( ; first < last; ++first)
    {
        result = sum (result; arr[i])
    }
    return result;
}
int main()
{
    int res = f(&[5]{1,2,3,4,5}; 1; 5; 3)
    cout << f << endl;
    return 0;
}
